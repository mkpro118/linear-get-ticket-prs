# Missing Prs

```
Find PRs present on main but missing from a release branch

Usage: missing-prs [OPTIONS]

Options:
  -b, --release-branch <RELEASE_BRANCH>
          The release branch to compare against (auto-detected from current branch if omitted)
  -h, --help
          Print help

```
